# 🧩 Monorepo 项目初始化指南

使用 Vite + Vue 3 构建现代前端 Monorepo。

配置 TypeScript + ESLint + Prettier + Husky + lint-staged

## 📦 使用 pnpm 初始化

### 安装 pnpm

```bash
npm install -g pnpm
```

### 初始化主项目
```bash
pnpm init
```

### 新建 pnpm-workspace.yaml

在根目录创建 pnpm-workspace.yaml，配置要管理的子包目录：
```yaml
packages:
  - "packages"  # 核心代码(core)
  - "play"      # 沙盒环境/开发/测试
  - "docs"      # 文档
```
支持多层目录结构，如：
```yaml
  - "test/"  # test/ui、test/utils
```

### 初始化子包
在对应文件夹中执行：

```bash
cd packages/play
pnpm init
```

## ⚙️ TypeScript 配置

在根目录安装 TypeScript：
```bash
pnpm add typescript -Dw
```

Vue 项目推荐同时安装 vue-tsc，用于类型检查：
```bash
pnpm add vue-tsc -Dw
```

新建 tsconfig.json
```json
{
    "files": [],
    "references": [
        { "path": "./packages" },
        { "path": "./play" },
        { "path": "./docs" }
    ],
    "compilerOptions": {
        "composite": true,
        "declaration": true,
        "declarationMap": true,
        "sourceMap": true,
        "outDir": "dist",
        "module": "ESNext",
        "target": "ESNext",
        "moduleResolution": "Bundler",
        "esModuleInterop": true,
        "strict": true,
        "skipLibCheck": true,
        "baseUrl": ".",
        "rootDir": ".",
        "paths": {
            "packages/*": ["packages/*"]
        }
    }
}
```

子包可以继承主包的 tsconfig.json：

```json
{
  "extends": "../tsconfig.json",
  "include": ["src"]
}
```

## 使用 Jiti 运行配置文件

在项目中，可能会使用 TypeScript 编写配置文件（如 vite.config.ts、eslint.config.ts、scripts/build.ts 等），
但 Node.js 默认无法直接执行 .ts 文件。
我们可以使用 Jiti 实现 即时编译执行：

```bash
pnpm add jiti -Dw
```

## 🧹 ESLint 配置

```bash
pnpm add eslint eslint-plugin-unused-imports eslint-plugin-vue @vue/eslint-config-prettier @vue/eslint-config-typescript -Dw
```

### 推荐插件说明
| 包名                                | 作用                      | 必须    | 说明                                   |
| --------------------------------- | ----------------------- | ----- | ------------------------------------ |
| **eslint**                        | 核心 ESLint 引擎            | ✅ 必须  | 提供规则系统与插件机制                          |
| **eslint-plugin-vue**             | Vue 官方插件，支持 `.vue` 文件检查 | ✅ 必须  | 启用 `plugin:vue/vue3-recommended` 等规则 |
| **eslint-plugin-unused-imports**  | 自动清理未使用的 `import`       | ⚙️ 可选 | 搭配 `eslint --fix` 自动清理               |
| **@vue/eslint-config-typescript** | Vue + TS 规则集            | ✅ 推荐  | 集成 `@typescript-eslint`              |
| **@vue/eslint-config-prettier**   | 禁用与 Prettier 冲突的规则      | ✅ 推荐  | 保证两者协同工作                             |

### 新建 eslint.config.ts

```ts
import { globalIgnores } from 'eslint/config';
import { defineConfigWithVueTs, vueTsConfigs } from '@vue/eslint-config-typescript';
import pluginVue from 'eslint-plugin-vue';
import skipFormatting from '@vue/eslint-config-prettier/skip-formatting';
import UnusedImports from 'eslint-plugin-unused-imports';

export default defineConfigWithVueTs(
  globalIgnores(['**/dist/**', '**/dist-ssr/**', '**/coverage/**']),

  pluginVue.configs['flat/essential'],
  vueTsConfigs.recommended,
  skipFormatting,

  {
    name: 'app/files-to-lint',
    files: ['**/*.{ts,mts,tsx,vue}'],
    plugins: {
      'unused-imports': UnusedImports,
    },
    rules: {
      'vue/multi-word-component-names': 'off',
      '@typescript-eslint/no-explicit-any': 'off',
      '@typescript-eslint/no-unused-vars': 'error',
      // "@typescript-eslint/no-unused-expressions": "off",
      'unused-imports/no-unused-imports': 'error',
    },
  },
);
```

## 🎨 Prettier 配置

```bash
pnpm add prettier -Dw
```
### 新建 .prettierrc.json

```json
{
  "$schema": "https://json.schemastore.org/prettierrc",
  "semi": true,
  "singleQuote": true,
  "printWidth": 100
}
```

## 💡 VSCode 配置

在根目录创建 .vscode 文件夹，并添加以下文件：

`.vscode/extensions.json`

```json
{
  "recommendations": [
    "Vue.volar",
    "dbaeumer.vscode-eslint",
    "EditorConfig.EditorConfig",
    "esbenp.prettier-vscode"
  ]
}
```

`.vscode/settings.json`
```json
{
  "editor.codeActionsOnSave": {
    "source.fixAll.eslint": "explicit"
  },
  "eslint.validate": ["javascript", "typescript", "vue"],
  "editor.formatOnSave": true,
  "editor.defaultFormatter": "esbenp.prettier-vscode",
  "[vue]": {
    "editor.defaultFormatter": "esbenp.prettier-vscode"
  }
}
```

在根目录创建 `.editorconfig`
```ini
[*.{js,jsx,mjs,cjs,ts,tsx,mts,cts,vue,css,scss,sass,less,styl}]
charset = utf-8
indent_size = 2
indent_style = space
insert_final_newline = true
trim_trailing_whitespace = true

end_of_line = lf
max_line_length = 100
```

## ⚡ Vite + Vue 3 子包配置

进入子包（例如 `play`）：
```bash
cd play
pnpm add vue
pnpm add vite @vitejs/plugin-vue -D
```

修改 play/package.json：
```json
{
  "name": "play",
  "private": true,  // 
  "type": "module", // 
  "scripts": {
    "dev": "vite"
  },
  "packageManager": "pnpm@10.14.0",
  "dependencies": {
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5",
  }
}
```
创建以下文件：
```csharp
play/
 ├─ public/
 ├─ index.html
 └─ src/
    ├─ App.vue
    └─ main.ts
```

### 在play子包中链接packages子包依赖

```json
{
    "name": "play",
  "private": true,  // 
  "type": "module", // 
  "scripts": {
    "dev": "vite"
  },
  "packageManager": "pnpm@10.14.0",
  "dependencies": {
    "vue": "^3.5.22"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "vite": "^6.3.5",

    // 新增子包，packages要与子包同名
    "packages": "workspace:*"
  }
}
```
添加后，在根目录运行：
```bash
pnpm install
```
## 🪄 Husky + lint-staged 配置

安装：
```bash
pnpm add husky lint-staged -Dw
```

初始化 Git 与 Husky
```bash
git init  # 已有git 则不需要
npx husky init
```
新增脚本命令
```json
{
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "type-check": "vue-tsc --build",
        "lint": "eslint .",
        "lint:fix": "eslint . --fix",
        "format": "prettier --write packages/**/*.{ts,tsx,vue} play/src/*.{ts,tsx,vue}",
        "play": "pnpm --filter play dev",
        
        // 新增命令
        "precommit": "pnpm run type-check && pnpm lint-staged",
    },
    "lint-staged": {
        "*.{js,ts,vue}": [
        "eslint --fix",
        "prettier --write"
        ]
    },
}
```
编辑 .husky/pre-commit
```sh
#!/bin/sh
. "$(dirname "$0")/_/husky.sh"

echo "🔍 Running precommit" >&2

pnpm run precommit
```

这样，当执行 git commit 时，暂存区代码会自动执行 ESLint + Prettier 校验与格式化。

✅ 总结
Monorepo 结构应如下所示：

```csharp
project/
├─ .husky/                        # Husky 钩子目录（Git 提交前自动执行 ESLint、Prettier 等）
│   └─ pre-commit                 # 提交前触发的脚本（执行 lint-staged）
│
├─ .vscode/                       # VSCode 项目专用配置
│   ├─ extensions.json            # 推荐安装的插件（Volar、ESLint、Prettier 等）
│   └─ settings.json              # VSCode 编辑器规则（保存时自动格式化、ESLint 校验等）
│
├─ packages/                      # 核心包目录（存放核心库、组件或逻辑代码）
│   ├─ core/                      # 核心包（例如组件库、工具库等）
│   ├─ index.ts                   # 汇总导出（可集中导出多个子包）
│   ├─ tsconfig.json              # packages 层级的 TypeScript 配置（方便各包共享）
│   └─ package.json               # packages 层级定义（可声明内部依赖关系）
│
├─ play/                          # 沙盒项目，用于本地开发和测试 core 包
│   ├─ public/                    # 静态资源目录（favicon、静态图片等）
│   ├─ src/                       # Vue 应用源码目录
│   │   ├─ App.vue                # 根组件
│   │   └─ main.ts                # 应用入口文件
│   ├─ index.html                 # Vite 启动入口 HTML
│   ├─ vite.config.ts             # Vite 配置文件（使用 @vitejs/plugin-vue）
│   ├─ tsconfig.json              # play 子包的 TypeScript 配置（继承根配置）
│   └─ package.json               # play 子包依赖与脚本定义
│
├─ docs/                          # 项目文档目录（可放文档站点、Storybook 或 VitePress）
│
├─ .gitignore                     # Git 忽略规则（忽略 node_modules、dist、缓存文件等）
├─ .editorconfig                  # 跨编辑器统一代码风格（缩进、换行符、编码等基础规范）
├─ package.json                   # 根包配置（管理工作区依赖、Husky、lint-staged、全局命令）
├─ eslint.config.ts               # ESLint 配置文件（定义 Vue + TS 校验规则）
├─ .prettierrc.json               # Prettier 配置文件（代码格式化规则）
├─ pnpm-workspace.yaml            # pnpm 工作区定义文件（列出参与 Monorepo 的子包目录）
└─ tsconfig.json                  # 根 TypeScript 配置（通用规则 + 引用各子包）
```